server:
  port: 8080
spring:
  profiles:
    active: dev
#  application:
#    name: api-server
  cloud:
    config:
      name: config-dev
      label: master
      profile: dev
      uri: http://localhost:8091